# 2026-08-11

## netcup2 public HTTP/HTTPS route repair

- Investigated `outreach-dev.rococo.dev` loading indefinitely through the operator's v2rayN TUN.
- Confirmed the application, PostgreSQL, Nginx, DNS, certificate, and local API responses were healthy on `netcup2`.
- Compared `netcup1` and `netcup2`: `netcup2` had a host-level sing-box TUN with policy table `2022`, while only TCP source port 22 bypassed the TUN through the main routing table.
- Verified the failure was asymmetric routing: HTTP/HTTPS responses entered `singtun0`; SSH worked because port 22 already had a route exception.
- Added persistent IPv4 and IPv6 main-table rules for TCP source ports 80 and 443 in `/etc/systemd/system/sing-box.service.d/public-service-routes.conf`.
- Reloaded systemd and restarted sing-box. The service remained active and priorities 8997, 8998, and 8999 were present before sing-box automatic rules.
- Post-restart checks passed: server-local HTTP/HTTPS returned 200, the current v2rayN SOCKS path returned 200 for the homepage and template API, and the default client path returned 200.
- No application deployment, database change, contact mutation, or email send occurred during this repair.

## Product and delivery documentation reconciliation

- Reconciled the product plan, sender status, and Tencent SMTP operations notes
  with the deployed implementation and the 2026-08-09/10 verification evidence.
- Recorded that protected Tencent SMTP Campaign sending, Campaign/Delivery
  persistence, exact sender-to-mailbox validation, idempotency, manual retry,
  partial-failure recovery, and Sent-folder verification are complete for the
  current development mailbox.
- Recorded the current architecture boundary: contacts, templates, campaigns,
  deliveries, and retries are persistent, while the Compose draft remains local
  browser state.
- Replaced the outdated Resend-first recommendation with the verified Tencent
  enterprise mailbox SMTP path while retaining a provider-neutral transport
  boundary.
- Clarified the next gates before broader use: application-wide access control,
  HTML sanitization, automated browser coverage, rate limiting/queue controls,
  and persistent mailbox-account authorization for multiple senders.
- Documentation-only change: no application deployment, database change,
  contact mutation, template mutation, or email send occurred.
