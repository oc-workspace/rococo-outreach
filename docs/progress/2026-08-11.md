# 2026-08-11

## netcup2 public HTTP/HTTPS route repair

- Investigated `outreach-dev.rococo.dev` loading indefinitely through the operator's v2rayN TUN.
- Confirmed the application, PostgreSQL, Nginx, DNS, certificate, and local API responses were healthy on `netcup2`.
- Compared `netcup1` and `netcup2`: `netcup2` had a host-level sing-box TUN with policy table `2022`, while only TCP source port 22 bypassed the TUN through the main routing table.
- Verified the failure was asymmetric routing: HTTP/HTTPS responses entered `singtun0`; SSH worked because port 22 already had a route exception.
- Added persistent IPv4 and IPv6 main-table rules for TCP source ports 80 and 443 in `/etc/systemd/system/sing-box.service.d/public-service-routes.conf`.
- Reloaded systemd and restarted sing-box. The service remained active and priorities 8997, 8998, and 8999 were present before sing-box automatic rules.
- Post-restart checks passed: server-local HTTP/HTTPS returned 200, the current v2rayN SOCKS path returned 200 for the homepage and template API, and the default client path returned 200.
- No application deployment, database change, contact mutation, or email send occurred during this repair.

## Product and delivery documentation reconciliation

- Reconciled the product plan, sender status, and Tencent SMTP operations notes
  with the deployed implementation and the 2026-08-09/10 verification evidence.
- Recorded that protected Tencent SMTP Campaign sending, Campaign/Delivery
  persistence, exact sender-to-mailbox validation, idempotency, manual retry,
  partial-failure recovery, and Sent-folder verification are complete for the
  current development mailbox.
- Recorded the current architecture boundary: contacts, templates, campaigns,
  deliveries, and retries are persistent, while the Compose draft remains local
  browser state.
- Replaced the outdated Resend-first recommendation with the verified Tencent
  enterprise mailbox SMTP path while retaining a provider-neutral transport
  boundary.
- Clarified the next gates before broader use: application-wide access control,
  HTML sanitization, automated browser coverage, rate limiting/queue controls,
  and persistent mailbox-account authorization for multiple senders.
- Documentation-only change: no application deployment, database change,
  contact mutation, template mutation, or email send occurred.

## Application access control and HTML safety

- Added a single login gate for all application pages and APIs by reusing the
  existing server-configured operator token.
- Added signed 12-hour `HttpOnly`, `Secure`, `SameSite=Strict` browser sessions,
  a login page, and explicit sign-out while preserving Bearer-token access for
  operational scripts.
- Unauthenticated pages now redirect to `/login`; unauthenticated contact, CSV
  import, sender, template, Campaign, delivery, send, and retry API requests
  return `401` when the development environment is configured.
- Removed the duplicate operator-token input from Compose. The raw token is not
  persisted in browser storage or application state after login.
- Added an HTML allowlist that preserves supported email formatting while
  removing executable tags, event attributes, inline styles, iframes, and
  unsafe URL schemes.
- Applied sanitization before template and Campaign storage, when returning
  existing stored content, during recipient preview rendering, and again at
  the final SMTP send boundary. Plain-text content is regenerated from the
  sanitized HTML before persistence and delivery.
- Added unit coverage for supported formatting, dangerous HTML/URL removal,
  hardened external links, bounded session expiry, session tampering, and exact
  Bearer-token matching.
- Local verification passed: six unit tests, production build, login redirect,
  unauthenticated API rejection, invalid-token rejection, authenticated browser
  page access, and session/Bearer API pass-through.
- This implementation and local verification did not connect to the deployment
  database, mutate contacts or templates, or send email. The 13 previously sent
  `japan-media` contacts remain intentionally `inactive`.
- Pushed commit `0e8eff0` to `oc-workspace/rococo-outreach` and deployed it to
  `netcup2` with the existing restricted `oc-deploy` workflow. The application
  and PostgreSQL containers were healthy after deployment.
- Public post-deployment checks passed: `/login` returned `200`, the
  unauthenticated homepage redirected to `/login`, and unauthenticated contact,
  template, sender, and Campaign APIs returned `401` with private/no-store
  caching.
- An in-container session login using the existing environment credential
  returned `200`. Authenticated read-only checks confirmed 19 contacts, one
  active template, and 13 `japan-media` contacts, all still `inactive`.
- Deployment verification did not mutate the database or call any SMTP verify,
  test-send, Campaign-send, or retry operation.
