# 2026-08-12

## Isolated Playwright browser baseline

- Added Playwright `1.55.0` with Chromium coverage for the authenticated
  outreach application.
- Added a self-contained E2E runner that starts PostgreSQL 16 and Next.js on
  random loopback ports, applies the real Prisma migrations, resets deterministic
  fixtures before every test, and removes the temporary database on exit.
- Added a test-only `OUTREACH_MAIL_TRANSPORT=simulated` mode for Campaign send
  and retry. The default remains the existing Tencent SMTP transport when the
  variable is absent.
- Added browser coverage for login/API protection; CSV preview, invalid rows,
  import, and repeated import; filters, select-visible, and blocked contacts;
  Sender selection/recovery; and template persistence/application.
- Added Campaign coverage for preview, second confirmation, simulated send,
  idempotent repeated requests, partial failure persistence, and manual retry
  of only the failed recipient.
- Verified that the server regenerates delivery plain text from sanitized HTML
  rather than accepting client-supplied text.
- Added stable label/control associations for Sender, Campaign, and Recipient
  fields used by both assistive technology and browser tests.
- Added database-backed template editing, archive/restore, language/purpose/tag
  metadata, version creation, version history, and switching between versions.
- Added server-side template field validation and HTML allowlist enforcement for
  all template create/edit/version operations.
- Added browser coverage for template editing, metadata, version creation,
  history, switching, archive, and restore.
- Added a database-backed Campaign draft record with authenticated GET/PUT/DELETE
  APIs, server-side validation and HTML sanitization, debounced autosave, and
  refresh recovery for the campaign name, subject, body, sender, reply-to, and
  recipient rows.
- Added browser coverage for draft autosave and refresh recovery.
- Final isolated run passed all 10 Playwright tests. TypeScript checks, unit tests
  (8/8), and the production build also passed. No deployed database,
  contact, template, Campaign, or mailbox was accessed; no real email was sent.
- Pushed commit `870f1c4` and deployed it to `netcup2` with the existing
  restricted deployment workflow. The rebuilt application container and the
  existing PostgreSQL container were healthy after the switch.
- Confirmed the new application container does not define
  `OUTREACH_MAIL_TRANSPORT`; deployed Campaign sends therefore retain the
  Tencent SMTP default rather than the Playwright simulated transport.
- Public read-only checks passed: `/login` returned `200`, the unauthenticated
  homepage redirected to login, and unauthenticated contact, template, and
  Campaign APIs returned `401`.
- Authenticated read-only checks still returned 19 contacts, one active
  template, and 13 `japan-media` contacts, all `inactive`. Deployment
  verification did not call SMTP verify, test-send, Campaign send, or retry.

## Database template management deployment

- Added Prisma migration `20260812100000_add_template_versions_metadata` for
  template families, versions, current-version tracking, language, purpose, and
  tags. Existing templates migrate as version 1 and remain active/current.
- Added authenticated template APIs for editing, creating versions, listing
  version history, switching versions, archiving, and restoring.
- Deployed commit `a0880c1` to `netcup2` on 2026-08-12 after the production
  image build completed successfully.
- Full isolated Playwright regression passed `9/9`; unit tests passed `8/8`,
  TypeScript validation passed, and `yarn build` passed.
- Post-deploy read-only checks passed: `/login` returned `200`, unauthenticated
  homepage redirected to `/login`, and unauthenticated `/api/templates`
  returned `401`.
- Authenticated read-only data remained 19 contacts, one active current
  template (version 1), and 13 `japan-media` contacts, all `inactive`.
- No SMTP verification, test-send, Campaign send, retry, contact mutation, or
  template mutation was performed during deployment verification.

## Campaign draft persistence implementation

- Added Prisma migration `20260812130000_add_campaign_drafts` with one
  workspace-scoped draft snapshot. Existing Campaign and delivery records are
  unchanged.
- Draft autosave uses a 500 ms debounce and displays `Saving draft...`,
  `Draft saved`, or a server error in the Compose panel.
- The isolated test suite applies the new migration against temporary PostgreSQL
  and uses simulated transport; no real email or deployed data was accessed.

## Deployment status

- Commit `df432d9` is pushed to `oc-workspace/rococo-outreach` and the deployment
  host source tree is synced to it.
- Deployment was attempted twice. Both attempts stopped while resolving the
  existing `node:20-alpine` base image from Docker Hub (TLS timeout, then `401
  Unauthorized`). The application container was not replaced and remains on the
  previously deployed template-management version.
- The deployed container still reports `OUTREACH_MAIL_TRANSPORT=unset` and is
  healthy. No production database migration or Campaign draft write occurred.
- Retry deployment after Docker Hub registry access is restored; perform the
  same read-only checks before using the new draft endpoint in production.
